<h1>List of Categories</h1>

<% @categories.each do |category| %>
 <%= link_to category.name,category_path(category) %>
 <br>
<% end %>

<h2><%= link_to "Create New Category" , new_category_path %> </h2>
